import React, { Compenent } from 'react';
import { Text, View } from 'react-native';

export class luchtInfo extends React.Component {
    render() {
        return (
            <View>
                <Text> luchtInfo</Text>
            </View>
        );
    }
}